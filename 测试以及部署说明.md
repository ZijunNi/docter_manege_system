# 住院医师日程管家 — 启动说明

## 环境要求

- Node.js >= 18
- npm >= 9

## 安装依赖

```bash
cd docter_manege_system
npm install
```

## 开发模式

```bash
# 启动开发服务器（默认 localhost:5173）
npm run dev

# 如果需要局域网访问（手机测试）
npm run dev -- --host
```

启动后终端会显示访问地址，例如：
```
➜  Local:   http://localhost:5173/
➜  Network: http://192.168.x.x:5173/
```

开发模式下支持热更新（HMR），修改代码后浏览器自动刷新。

**停止服务器**：在终端按 `Ctrl + C`。

## 生产构建

```bash
# 构建到 dist/ 目录（base=/，适合本地预览）
npm run build:local

# 构建到 dist/ 目录（base=/docter_manege_system/，适合 GitHub Pages 部署）
npm run build
```

## 本地预览生产构建

```bash
npm run preview:local
```

这会先构建再启动预览服务器，适合验证 PWA Service Worker 和离线功能。

## 在手机上测试

1. 电脑和手机连接同一个 WiFi
2. 运行 `npm run dev -- --host`
3. 查看终端输出的 Network 地址（如 `http://192.168.x.x:5173/`）
4. 手机浏览器访问该地址

## 清除数据重新开始

- **浏览器中**：DevTools → Application → IndexedDB → 右键 `ResidentScheduleDB` → Delete database
- **应用中**：设置页 → 🗑 清空所有数据

## 开发模式（模拟日期）

在设置页开启「开发模式」后，可以手动指定"今天"的日期，方便测试不同场景下的任务生成：

1. 设置 → 打开「开发模式」开关
2. 选择一个模拟日期
3. 页面自动刷新，所有任务按模拟日期重新生成
4. 顶部会显示橙色提示条，标明当前模拟日期
5. 关闭开关即恢复正常日期

## 项目结构

```
src/
├── types/          # 数据模型（Patient, Task, EventType 等）
├── db/             # IndexedDB 数据库 + 种子数据
├── engine/         # 核心引擎（状态机、任务生成、节假日计算）
├── services/       # 业务服务层
├── hooks/          # React Hooks
├── components/     # UI 组件
│   ├── layout/     # AppShell, Header, BottomNav
│   ├── patient/    # 患者相关组件
│   ├── task/       # 任务相关组件
│   ├── event/      # 事件模板相关组件
│   └── ui/         # 通用 UI 组件
├── pages/          # 页面组件
├── router/         # 路由配置
└── utils/          # 工具函数
```

## 技术栈

| 层级 | 技术 |
|------|------|
| 框架 | React 19 + TypeScript 6 |
| 构建 | Vite 8 |
| 样式 | Tailwind CSS 4 |
| 路由 | React Router v7 (HashRouter) |
| 数据库 | IndexedDB (Dexie.js 4) |
| PWA | vite-plugin-pwa |
