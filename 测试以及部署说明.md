# 住院医师日程管家 — 启动说明

## 环境要求

- Node.js >= 18
- npm >= 9

在Windows端可能需要先声明环境变量：
```
$env:Path += ";C:\Program Files\nodejs"
```
## 安装依赖

```bash
cd docter_manege_system
npm install
```

## 开发模式

```bash
# 启动开发服务器（默认 localhost:5173）
npm run dev

# 如果需要局域网访问（手机测试）
npm run dev -- --host
```

启动后终端会显示访问地址，例如：
```
➜  Local:   http://localhost:5173/
➜  Network: http://192.168.x.x:5173/
```

开发模式下支持热更新（HMR），修改代码后浏览器自动刷新。

**停止服务器**：在终端按 `Ctrl + C`。

## 生产构建

```bash
# 构建到 dist-local/ 目录（base=/，适合本地预览）
npm run build:local

# 构建到 dist/ 目录（base=/docter_manege_system/，适合 GitHub Pages 部署）
npm run build
```

## 本地预览生产构建

### 预览本地路径版本

```bash
npm run preview:local
```

这会使用相同的根路径配置构建并预览独立的 `dist-local/`。请访问终端显示的地址，默认是：

```text
http://localhost:4173/
```

该命令适合验证 PWA Service Worker 和离线功能，且不会覆盖用于 GitHub Pages 部署的 `dist/`。

### 预览 GitHub Pages 子路径版本

```bash
npm run build
npm run preview
```

请访问：

```text
http://localhost:4173/docter_manege_system/
```

`npm run preview` 使用端口 `4173`，不要继续打开开发服务器的 `localhost:5173`。`dist/` 仅用于 `/docter_manege_system/` 子路径构建，`dist-local/` 仅用于根路径本地预览，两种产物不会再相互覆盖。

## 更新程序后的干净测试

每次更新源码、依赖、数据库版本或 PWA 配置后，建议按以下顺序测试，避免旧进程、旧构建产物和 Service Worker 缓存干扰结果。

1. **备份需要保留的数据**

   在应用设置页执行“导出数据备份”。如果准备测试数据迁移，保留导入前和导入后的两份备份，便于比较。

2. **停止旧服务器并关闭旧页面**

   在运行 `npm run dev` 或 `npm run preview` 的终端按 `Ctrl + C`。关闭所有 `localhost:5173` 和 `localhost:4173` 页面，避免旧页面继续控制数据库或 Service Worker。

3. **清理旧 Service Worker 和缓存**

   在浏览器 DevTools → Application 中，对 `localhost:5173` 和 `localhost:4173` 分别执行：

   - Service Workers → `Unregister`
   - Cache Storage → 删除该站点下的缓存

   如果只想更新程序而保留患者数据，不要删除 IndexedDB。如果需要从完全空的数据环境开始测试，再删除 IndexedDB 中的 `ResidentScheduleDB`；此操作会清除全部本地患者和任务数据。

4. **按锁文件重新安装依赖**

   ```bash
   npm ci
   ```

5. **执行本地生产预览测试**

   ```bash
   npm run preview:local
   ```

   打开终端显示的 `http://localhost:4173/`，确认页面能够加载、内置事件模板各只有一组，并测试患者新增、编辑、任务生成、数据导入和导出。

6. **执行部署路径测试**

   停止上一步的预览服务器，然后运行：

   ```bash
   npm run build
   npm run preview
   ```

   打开 `http://localhost:4173/docter_manege_system/`，在 DevTools → Network 中确认 JS、CSS、manifest 和 Service Worker 均返回 `200`，并确认地址栏刷新后仍能正常显示页面。

7. **测试 PWA 更新**

   保持预览页面打开，停止服务器、修改版本后重新构建并启动预览。刷新页面并确认新资源生效；如需验证离线模式，再在 DevTools → Network 中切换为 Offline。

开发模式 `npm run dev` 不注册 Service Worker，仅用于日常 HMR 调试；PWA 缓存和离线能力应使用 `npm run preview:local` 验证。

## 在手机上测试

1. 电脑和手机连接同一个 WiFi
2. 运行 `npm run dev -- --host`
3. 查看终端输出的 Network 地址（如 `http://192.168.x.x:5173/`）
4. 手机浏览器访问该地址

## 部署到 GitHub Pages

### 前置条件

- 项目已推送到 GitHub 仓库
- GitHub 仓库 Settings → Pages 中：
  - Source 设为 `Deploy from a branch`
  - Branch 选 `gh-pages`，目录选 `/ (root)`

### 部署步骤

```bash
# 1. 提交所有修改到 main 分支
git add -A
git commit -m "你的提交信息"

# 2. 推送到 GitHub
git push origin main

# 3. 一键构建并部署
npm run deploy
```

`npm run deploy` 会依次执行：
1. `npm run predeploy` — TypeScript 编译 + Vite 生产构建（base 路径自动设为 `/docter_manege_system/`）
2. `gh-pages -d dist` — 将 `dist/` 目录推送到 `gh-pages` 分支

部署完成后等待 1-2 分钟，访问：**https://{你的用户名}.github.io/docter_manege_system/**

### 如何检查部署状态

- GitHub 仓库 → Actions 标签页可查看部署进度
- Settings → Pages 顶部会显示部署状态和 URL

### 排查进程占用

```bash
# 检查 Vite 开发服务器 (端口 5173)
lsof -i :5173

# 检查 Vite 预览服务器 (端口 4173)
lsof -i :4173

# 杀掉占用端口的进程
lsof -ti :5173 | xargs kill
lsof -ti :4173 | xargs kill

# 杀掉所有 vite 相关进程
pkill -f vite
```

## 清除数据重新开始

- **浏览器中**：DevTools → Application → IndexedDB → 右键 `ResidentScheduleDB` → Delete database
- **应用中**：设置页 → 🗑 清空所有数据

## 开发模式（模拟日期）

在设置页开启「开发模式」后，可以手动指定"今天"的日期，方便测试不同场景下的任务生成：

1. 设置 → 打开「开发模式」开关
2. 选择一个模拟日期
3. 页面自动刷新，所有任务按模拟日期重新生成
4. 顶部会显示橙色提示条，标明当前模拟日期
5. 关闭开关即恢复正常日期

## 项目结构

```
src/
├── types/          # 数据模型（Patient, Task, EventType 等）
├── db/             # IndexedDB 数据库 + 种子数据
├── engine/         # 核心引擎（状态机、任务生成、节假日计算）
├── services/       # 业务服务层
├── hooks/          # React Hooks
├── components/     # UI 组件
│   ├── layout/     # AppShell, Header, BottomNav
│   ├── patient/    # 患者相关组件
│   ├── task/       # 任务相关组件
│   ├── event/      # 事件模板相关组件
│   └── ui/         # 通用 UI 组件
├── pages/          # 页面组件
├── router/         # 路由配置
└── utils/          # 工具函数
```

## 技术栈

| 层级 | 技术 |
|------|------|
| 框架 | React 19 + TypeScript 6 |
| 构建 | Vite 8 |
| 样式 | Tailwind CSS 4 |
| 路由 | React Router v7 (HashRouter) |
| 数据库 | IndexedDB (Dexie.js 4) |
| PWA | vite-plugin-pwa |
